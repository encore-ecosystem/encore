import core::testing::*
import std::path::Path

fn main() -> u32 {
    let cwd = Path::cwd()

    if !assert(cwd.as_str().len() > 0_usize) {
        ret 1_u32
    }

    if !assert(cwd.exists()) {
        ret 1_u32
    }

    ret 0_u32
}
