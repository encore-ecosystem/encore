import core::testing::*

macro_rules! forty_two_inner {
    () => (42_u32);
}

macro_rules! forty_two {
    () => (forty_two_inner!());
}

fn main() -> u32 {
    let value = forty_two!()
    if !assert(value == 42_u32) {
        ret 1_u32
    }
    ret 0_u32
}
