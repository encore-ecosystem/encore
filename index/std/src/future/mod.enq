pub import core::future::{Future, Poll, Ready, ready}

pub fn block_on[T, F: Future[T]](future: F) -> T {
    match future.poll() {
        Poll[F, T]::Ready(value) => { ret value }
        Poll[F, T]::Pending(next) => { ret block_on[T, F](next) }
    }
}
