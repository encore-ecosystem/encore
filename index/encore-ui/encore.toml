[project]
name = "encore_ui"
version = "0.1.0"
description = "Retained cross-platform native UI toolkit for Encore"
readme = "README.md"
licence = "MIT"
build = "build.enq"
dependencies = [
    "path@../color",
    "path@../geometry",
    "workspace@ui_native",
]

[features]
default = []
fonts = []
