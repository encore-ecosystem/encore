import core::testing::*
import std::path::Path
import std::string::len

fn main() -> u32 {
    let cwd = Path::cwd()

    if !assert(len(cwd.as_str()) > 0_usize) {
        ret 1_u32
    }

    if !assert(cwd.exists()) {
        ret 1_u32
    }

    ret 0_u32
}
