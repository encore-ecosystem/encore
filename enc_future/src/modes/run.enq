import std::io::println
import repo::modes::build::run_build

pub fn run_run() -> u32 {
    let status = run_build()
    if (status != 0_u32) {
        ret status
    }

    println("run: launch is not implemented yet")
    ret 0_u32
}
