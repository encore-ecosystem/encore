struct Vec2 {
    x: u32
    y: u32
}

fn make_vec(x: u32, y: u32) -> Vec2 {
    ret Vec2{x, y}
}

fn sum_coords(v: Vec2) -> u32 {
    ret v.x + v.y
}

fn run() -> u32 {
    let p: Vec2 = make_vec(10, 32)
    ret sum_coords(p)
}
