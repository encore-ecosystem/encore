struct Vector3D {
    x: u32
    y: u32
    z: u32
}

fn scalar_dot(a: Vector3D<H>, b: Vector3D<H>) -> u32 {
    ret a.x * b.x + a.y * b.y + a.z * b.z
}

fn main() -> u32 {
    let a = Vector3D<H>{1, 2, 3}
    let b = Vector3D<H>{4, 5, 6}
    ret scalar_dot(a, b)
}
