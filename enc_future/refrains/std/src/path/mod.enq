import refrain::{
    vec::Vec,
    string::String
}

pub struct Path {
    parts: Vec[String]
}


impl for Path {
    pub fn join(self: mut Self, part: str) -> Self {
        self.parts = self.parts.push(String::from_str(part))
        ret self
    }
}
