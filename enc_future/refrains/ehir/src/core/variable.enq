import ehir::core::types::Type

pub struct Parameter {
    name: str
    typ: Type
}

pub struct Variable {
    name: str
    typ: Type
}

pub fn param(name: str, typ: Type) -> Parameter {
    ret Parameter{name, typ}
}

pub fn var(name: str, typ: Type) -> Variable {
    ret Variable{name, typ}
}
