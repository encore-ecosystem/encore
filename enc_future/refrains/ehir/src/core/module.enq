import std::vec::Vec
import ehir::core::directives::Directive

pub struct Module {
    name: str
    directives: Vec[Directive]<H>
}

pub fn make_module(name: str, directives: Vec[Directive]<H>) -> Module {
    ret Module{name, directives}
}
