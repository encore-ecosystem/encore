import core::testing::*

fn main() -> u32 {
    let s = "hello"
    if !assert(s.len() == 5_usize) {
        ret 1_u32
    }
    if !assert(!s.is_empty()) {
        ret 1_u32
    }

    let empty = ""
    if !assert(empty.is_empty()) {
        ret 1_u32
    }
    ret 0_u32
}
