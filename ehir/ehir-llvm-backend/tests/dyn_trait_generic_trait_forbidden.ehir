;@expect.compile_error=dyn for generic trait
trait Store[T] {
  fn size(self: Self) -> usize
}

fn consume(v: dyn Store) -> usize {
  $entry:
    z = capprim 0_usize
    ret z
}

fn main() -> usize {
  $entry:
    z0 = capprim 0_usize
    ret z0
}
