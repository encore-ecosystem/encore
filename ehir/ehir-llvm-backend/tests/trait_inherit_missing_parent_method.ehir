;@expect.compile_error=misses method 'base'
trait Parent {
  fn base(self: Self) -> usize
}

trait Child < Parent {
  fn child(self: Self) -> usize
}

impl Child for usize {
  fn child(self: usize) -> usize {
    $entry:
      ret self
  }
}

fn main() -> usize {
  $entry:
    z0 = capprim 0_usize
    ret z0
}
