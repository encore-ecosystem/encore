import core::vec::Vec

fn count(values: sending Vec[str]) -> usize {
    ret values.len()
}

fn inspect(values: frozen Vec[str]) -> usize {
    ret values.len()
}

fn publish(values: sending Vec[str]) -> frozen Vec[str] {
    ret values
}

fn main() -> u32 {
    ret 0_u32
}
