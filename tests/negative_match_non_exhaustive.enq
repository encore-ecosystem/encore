//@expect.compile_error=:6:11: error[non-exhaustive-match]: non-exhaustive match for enum 'Option'
import core::option::Option

fn main() -> u32 {
    let value = Option[u32]::Some(1_u32)
    match value {
        Option::Some(x) => {
            ret x
        }
    }
}
