// @expect.compile_error=error[generic-arity-mismatch]: generic callable 'identity' expects 1 generic argument(s), got 2

fn identity[T](value: T) -> T {
    ret value
}

fn main() -> u32 {
    let value = identity[u32, str](1_u32)
    ret value
}
