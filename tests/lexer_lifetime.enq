import refrain::frontend::lexer::Lexer

fn main() -> u32 {
    let source = "fn main() -> u32 { ret 0_u32 }"
    let mut lexer = Lexer::new()
    let tokens = lexer.tokenize(source)
    if tokens.len() == 0_usize { ret 1_u32 }
    ret 0_u32
}
