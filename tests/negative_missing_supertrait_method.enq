// @expect.compile_error=error[missing-trait-method]: implementation of trait 'Child' for 'Value' is missing method 'base'

trait Base {
    fn base(self: Self) -> u32
}

trait Child < Base {
    fn child(self: Self) -> u32
}

struct Value {}

impl Child for Value {
    fn child(self: Self) -> u32 {
        ret 0_u32
    }
}

fn main() -> u32 {
    ret 0_u32
}
