// @expect.compile_error=requires a mutable receiver
import core::vec::Vec

struct State {
    values: Vec[str]
}

impl for State {
    fn add(self: State, value: str) -> () {
        self.values.push(value)
    }
}

fn main() -> u32 {
    ret 0_u32
}
