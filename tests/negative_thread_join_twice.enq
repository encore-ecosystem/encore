// @expect.compile_error=unknown variable 'handle'

fn worker() -> u32 { ret 1_u32 }

fn main() -> u32 {
    let handle = spawn worker()
    let first = handle.join()
    let second = handle.join()
    ret first + second
}
