// @expect.compile_error=error[invalid-try-context]: '?' operator can only be used in functions returning Result

fn value() -> Result[u32, str] {
    ret Result[u32, str]::Ok(1_u32)
}

fn main() -> u32 {
    ret value()?
}
