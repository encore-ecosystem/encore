// @expect.compile_error=error[invalid-match-payload]: pattern 'Unit::Only' has no payload compatible with type 'Unit'

enum Unit {
    Only
}

fn main() -> u32 {
    let value = Unit::Only
    match value {
        Unit::Only(payload) => {
            let ignored = payload
        }
    }
    ret 0_u32
}
