// @expect.compile_error=error[node-handle-required]: parameter requires an owning node handle; construct 'Node<S>' or 'Node<H>' explicitly

struct Node {
    value: u32
}

fn read(node: Node&) -> u32 {
    ret node.value
}

fn main() -> u32 {
    let node = Node {42_u32}
    ret read(node)
}
