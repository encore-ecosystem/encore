// @expect.compile_error=is not available through frozen region 'values'
import core::vec::Vec

fn mutate(values: frozen Vec[str]) -> usize {
    values.push("forbidden")
    ret values.len()
}

fn main() -> u32 {
    ret mutate(Vec[str]::new()) as u32
}
