// @expect.compile_error=error[trait-method-signature-mismatch]: parameter 2 of method 'combine' has type 'str', expected 'u32'

trait Combine {
    fn combine(self: Self, other: u32) -> u32
}

struct Value {}

impl Combine for Value {
    fn combine(self: Self, other: str) -> u32 {
        ret 0_u32
    }
}

fn main() -> u32 {
    ret 0_u32
}
