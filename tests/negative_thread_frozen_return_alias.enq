// @expect.compile_error=cannot return 'values' as frozen because its region may remain reachable
import core::vec::Vec

fn unsound(values: Vec[str]) -> frozen Vec[str] {
    ret values
}

fn main() -> u32 {
    ret unsound(Vec[str]::new()).len() as u32
}
