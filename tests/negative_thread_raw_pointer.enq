// @expect.compile_error=cannot cross a thread boundary

fn worker(value: u8*) -> usize { ret value as usize }

fn main() -> u32 {
    unsafe {
        let pointer = 0_usize as u8*
        let handle = spawn worker(pointer)
        let result = handle.join()
        ret result as u32
    }
}
