// @expect.compile_error=error[missing-trait-method]: implementation of trait 'Measure' for 'Value' is missing method 'measure'

trait Measure {
    fn measure(self: Self) -> u32
}

struct Value {}

impl Measure for Value {}

fn main() -> u32 {
    ret 0_u32
}
