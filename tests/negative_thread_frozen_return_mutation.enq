// @expect.compile_error=is not available through frozen region
import core::vec::Vec

fn publish(values: sending Vec[str]) -> frozen Vec[str] {
    ret values
}

fn main() -> u32 {
    let mut owned = Vec[str]::new()
    let mut published = publish(owned)
    published.push("mutation")
    ret 0_u32
}
