// @expect.compile_error=error[mutable-receiver-required]: method 'push' requires a mutable receiver
import std::vec::Vec

fn main() -> u32 {
    let values = Vec[u32]::new()
    values.push(1_u32)
    ret 0_u32
}
