// @expect.compile_error=requires a mutable receiver
import std::vec::Vec

fn main() -> u32 {
    let values = Vec[u32]::new()
    values.push(1_u32)
    ret if values.len() == 1_usize &&
        values.get(0_usize).unwrap_or(0_u32) == 1_u32 {
        0_u32
    } else {
        1_u32
    }
}
