fn decorate(value: str) -> str {
    ret value + " from worker"
}

fn main() -> u32 {
    let source = "hello"
    let handle = spawn decorate(source)
    let result = handle.join()
    if result != "hello from worker" { ret 1_u32 }
    ret 0_u32
}
