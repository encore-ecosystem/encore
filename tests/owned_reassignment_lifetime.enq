import std::vec::Vec

fn main() -> u32 {
    let mut current = Vec[str]::new()
    current.push("old")

    let mut next = Vec[str]::new()
    next.push("new")
    current = next

    if current.len() != 1_usize || next.len() != 1_usize {
        ret 1_u32
    }
    ret 0_u32
}
