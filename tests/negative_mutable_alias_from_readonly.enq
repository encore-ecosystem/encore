// @expect.compile_error=cannot create mutable binding 'alias' from immutable region 'values'
import core::vec::Vec

fn main() -> u32 {
    let values = Vec[str]::new()
    let mut alias = values
    alias.push("invalid")
    ret 0_u32
}
