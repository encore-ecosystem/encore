import core::testing::assert
import core::vec::Vec

fn make_text() -> str {
    ret "owned" + " temporary"
}

fn make_values() -> Vec[str] {
    ret Vec[str]::singleton("value")
}

fn main() -> u32 {
    make_text()
    make_values()
    ret if assert(true) { 0_u32 } else { 1_u32 }
}
