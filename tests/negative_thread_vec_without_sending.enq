// @expect.compile_error=can cross spawn only through a 'sending' parameter
import core::vec::Vec

fn count(values: Vec[str]) -> usize {
    ret values.len()
}

fn main() -> u32 {
    let values = Vec[str]::new()
    let worker = spawn count(values)
    ret worker.join() as u32
}
