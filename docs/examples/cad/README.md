# Encore CAD Workbench

A small CAD workbench example. It uses separate refrains for the modeling
kernel, project reporting and native GUI drawing. The GUI opens an X11 window
on Linux when a display is available and falls back to a textual summary
otherwise.

