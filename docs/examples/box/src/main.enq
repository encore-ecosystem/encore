import std::box::Box

struct Node {
    value: u32
}

fn get_val(node: Node&) -> u32 {
    ret node.value
}

fn main() -> u32 {
    let boxed: Box[Node] = Box[Node]::new(Node<H>{42_u32})
    ret get_val(boxed.as_ref())
}
