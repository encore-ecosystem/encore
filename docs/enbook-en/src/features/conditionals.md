# Conditional Control Flow

Encore uses `if`, `elif`, and `else`. Branches can return early, and an
`if ... else` whose branches end in values can be used as an expression when
their types agree.

```encore
{{#include ../../examples/guide/src/features/mod.enq:control_flow}}
```

