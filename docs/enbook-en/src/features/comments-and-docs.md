# Comments and Documentation

Use `//` and `/* ... */` for ordinary comments. `///` documents the next
public item, while `//!` documents the current module. Documentation is
available to the Encore LSP for hover information.

```encore
{{#include ../../examples/guide/src/features/mod.enq:comments}}
```

