# `std::vec`

Re-exports `Vec[T]` and adds convenience helpers such as `append`,
`vec_singleton`, `head_or`, and `tail_or`.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:vec}}
```

