# `std::random`

`Random` is a small deterministic generator. State transitions return both
the next generator and generated value, making reproducible sequences
explicit.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:random}}
```

