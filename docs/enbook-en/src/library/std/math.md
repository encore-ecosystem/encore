# `std::math`

Provides trigonometric functions and generic `min`, `max`, and `clamp`
operations over types with the required comparison traits.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:math}}
```

