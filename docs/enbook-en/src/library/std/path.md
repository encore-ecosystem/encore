# `std::path`

`Path` parses and normalizes platform-aware paths and supports joining,
absolute resolution, home expansion, parents, names, and filesystem helpers.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:path}}
```

