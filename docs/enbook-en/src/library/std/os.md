# `std::os`

Re-exports arguments, environment, host identity, filesystem primitives,
executable paths, checksums, and update helpers from the platform layer.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:os}}
```

