# `std::string`

`String` is an owned string wrapper with conversion, slicing, searching,
trimming, splitting, and `Debug` formatting methods.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:string}}
```

