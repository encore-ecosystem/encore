# `std::net`

Re-exports TCP streams, listeners, address formatting, and their
`ContextManager` cleanup implementations from the platform layer.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:net}}
```

