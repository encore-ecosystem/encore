# `std::process`

Re-exports process launching, captured output, exit codes, and the chainable
`Command` builder from the platform package.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:process}}
```

