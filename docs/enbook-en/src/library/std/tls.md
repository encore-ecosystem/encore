# `std::tls`

Re-exports TLS client configuration and streams. Connections, reads, writes,
and close operations return `Result` values.

```encore
{{#include ../../../examples/guide/src/std_examples/mod.enq:tls}}
```

