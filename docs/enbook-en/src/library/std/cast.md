# `std::cast`

Re-exports the core numeric `Cast[T]` and `Numeric` contracts for applications
that consistently import through `std`.

```encore
{{#include ../../../examples/guide/src/core_examples/mod.enq:cast}}
```

