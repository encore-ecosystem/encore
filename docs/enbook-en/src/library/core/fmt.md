# `core::fmt`

Defines `Debug`, the common conversion contract used by diagnostics,
`format!`, and debug-oriented I/O helpers.

```encore
{{#include ../../../examples/guide/src/core_examples/mod.enq:fmt}}
```

