# `core::testing`

Provides the lightweight `assert` and `fail` primitives used by
`#attr(test)` functions and standalone compiler regression programs.

```encore
{{#include ../../../examples/guide/src/core_examples/mod.enq:testing}}
```

