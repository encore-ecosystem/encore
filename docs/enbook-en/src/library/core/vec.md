# `core::vec`

`Vec[T]` is a growable contiguous collection with capacity management,
checked optional access, iteration, and explicit native-boundary access.

```encore
{{#include ../../../examples/guide/src/core_examples/mod.enq:vec}}
```

