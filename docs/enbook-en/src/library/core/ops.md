# `core::ops`

Defines operator traits (`Add`, `Eq`, `Pow`, shifts, indexing, and others) plus
`ContextManager`. Built-in operators and user implementations share these
contracts.

```encore
{{#include ../../../examples/guide/src/core_examples/mod.enq:ops}}
```

