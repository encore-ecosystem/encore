;@cfree.free_ids=manual
struct Payload {
  id: str
  value: usize
}

fn main() -> usize {
  $entry:
    id = capprim "manual"_str
    one = capprim 1_usize
    raw = capstruct Payload(id, one)
    boxed = wraph raw
    drop boxed
    zero = capprim 0_usize
    ret zero
}
