struct Payload {
  value: usize
}

fn main() -> usize {
  $entry:
    one = capprim 1_usize
    raw = capstruct Payload(one)
    boxed = wraph raw
    drop boxed
    zero = capprim 0_usize
    ret zero
}
