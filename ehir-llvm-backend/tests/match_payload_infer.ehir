enum Option[T] {
  Some(T)
  None
}

fn main() -> usize {
  $entry:
    two = capprim 2_usize
    val = capenum Option[usize]::None()
    match val, $none {
      Some(x) => $some
    }
  $some:
    sum = add x, two
    ret sum
  $none:
    zero = capprim 0_usize
    ret zero
}
