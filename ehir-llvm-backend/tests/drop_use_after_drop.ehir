;@expect.compile_error=Use-after-drop of 'boxed'
struct Payload {
  value: usize
}

fn main() -> usize {
  $entry:
    one = capprim 1_usize
    raw = capstruct Payload(one)
    boxed = wraph raw
    drop boxed
    ptr = getfield boxed > ptr
    zero = capprim 0_usize
    ret zero
}
