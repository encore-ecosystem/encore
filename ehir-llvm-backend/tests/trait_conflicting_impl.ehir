;@expect.compile_error=Conflicting generated directive for 'EqLike::usize'
trait EqLike {
  fn same(self: Self, other: Self) -> u1
}

impl EqLike for usize {
  fn same(self: usize, other: usize) -> u1 {
    $entry:
      v = capprim 1_u1
      ret v
  }
}

impl EqLike for usize {
  fn same(self: usize, other: usize) -> u1 {
    $entry:
      v = capprim 0_u1
      ret v
  }
}

fn main() -> usize {
  $entry:
    zero = capprim 0_usize
    ret zero
}
