enum Option[T] {
  Some(T)
  None
}

fn main() -> usize {
  $entry:
    one = capprim 1_usize
    val = capenum Option[usize]::Some(one)
    match val, $fallback {
      Some => $a
      None => $b
    }
  $a:
    zero = capprim 0_usize
    ret zero
  $b:
    one_fail = capprim 1_usize
    ret one_fail
  $fallback:
    two = capprim 2_usize
    ret two
}
