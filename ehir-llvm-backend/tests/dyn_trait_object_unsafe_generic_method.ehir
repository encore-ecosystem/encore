;@expect.compile_error=is not object-safe
trait Bad {
  fn map[T](self: Self, value: T) -> usize
}

fn take(v: dyn Bad) -> usize {
  $entry:
    z = capprim 0_usize
    ret z
}

fn main() -> usize {
  $entry:
    z = capprim 0_usize
    ret z
}
