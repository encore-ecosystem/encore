;@expect.compile_error=Trait inheritance supports exactly one parent
trait A {
  fn a(self: Self) -> usize
}

trait B {
  fn b(self: Self) -> usize
}

trait C < A, B {
  fn c(self: Self) -> usize
}

fn main() -> usize {
  $entry:
    z0 = capprim 0_usize
    ret z0
}
