;@expect.compile_error=returns Self
trait CloneLike {
  fn clone(self: Self) -> Self
}

fn take(v: dyn CloneLike) -> usize {
  $entry:
    z = capprim 0_usize
    ret z
}

fn main() -> usize {
  $entry:
    z = capprim 0_usize
    ret z
}
