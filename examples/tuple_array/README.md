Smoke example for Rust-like tuple/array syntax:
- tuple type/literal and tuple field access (`.0`, `.1`)
- array type/literal and fixed-size repeat literal (`[x; N]`)
- array indexing (`arr[IDX]`, currently constant integer index)
