fn raw_add(a: u32, b: u32) -> u32 {
    ehir {
        result: u32 = add a: u32, b: u32
    }
    ret result
}

fn main() -> u32 {
    let x: u32 = 5_u32

    unsafe ehir {
        loaded: u32 = load x: u32*
    }

    ret raw_add(loaded, 7_u32)
}
