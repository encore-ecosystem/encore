# CAD fonts

The CAD example uses X11 font aliases through `cad_ui::Window::set_font`.
Current profiles:

- `fixed`: compact default font.
- `9x15`: readable engineering panel font.
- `10x20`: large presentation font.

Use `F` or click font rows in the right inspector to switch at runtime.

