struct Vector2D {
    x: u32
    y: u32
}

fn vector_add(a: Vector2D, b: Vector2D) -> Vector2D {
    ret Vector2D {a.x + b.x, a.y + b.y}
}

fn main() -> u32 {
    let a : Vector2D = Vector2D {5, 2}
    let b : Vector2D = vector_add(a, a)
    ret b.x + b.y
}
