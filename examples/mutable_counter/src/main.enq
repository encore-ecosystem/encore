struct Counter {
    value: u32
}

impl for Counter {
    fn increment(self: mut Counter, amount: u32) -> () {
        self.value = self.value + amount
        ret ()
    }
}

fn main() -> u32 {
    let mut counter = Counter{40_u32}
    counter.increment(2_u32)
    ret counter.value
}
