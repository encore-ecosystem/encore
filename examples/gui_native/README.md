# gui_native

Native desktop window backend for Encore GUI examples.

This package wraps the low-level window ABI and exposes a small Encore API:
create a window, poll close events, clear, draw filled rectangles, present, and close.
