struct Node {
    value: u32
}

fn get_val(node: Node&) -> u32 {
    ret node.value
}

fn main() -> u32 {
    let heap_node = Node<H>{5_u32}
    let stack_node = Node<S>{10_u32}

    ret get_val(heap_node) + get_val(stack_node)
}
