pub struct WindowDesc {
    title: str
    width: u32
    height: u32
}

impl for WindowDesc {
    pub fn new(title: str, width: u32, height: u32) -> WindowDesc {
        ret WindowDesc { title, width, height }
    }
}
