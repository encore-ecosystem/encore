# Encore UI Demo

Build and run from this directory:

```sh
encore run
```

The demo requires an SDL3 runtime and exercises retained layout, styles,
rendering, pointer hit testing, and keyboard events. Press Escape to exit.
